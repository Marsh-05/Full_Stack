# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/leave_manager?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
# CHANGE THE PASSWORD BELOW TO YOUR ACTUAL MYSQL PASSWORD!
spring.datasource.password=W0lfsb@ne

# Hibernate (JPA) Settings
# This tells Spring Boot to automatically create or update our database tables!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect